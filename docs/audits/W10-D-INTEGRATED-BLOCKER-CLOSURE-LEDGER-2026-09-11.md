# W10-D Integrated Blocker Closure

Status: D0 SOURCE COMPLETE - INDEPENDENT REVIEW REQUIRED. D1-D6 HOLD.
Executor record, not an independent verdict or Director approval.

## Binding

- Source baseline: `a8d72b8e2a6e78438e4cef46ad21913ff369d3f0`.
- Baseline tree: `75364250f2a5bbe875fc4bcf3da517c8f456adea`.
- Staging: `777314b12bcd168e2772603a31d657919aafa5bb`.
- Main: `418846959b90b38e10141cb8df995872802219fe`.
- Branch: `fix/w10d-integrated-blocker-closure-20260911`.
- All three referenced report/evidence hashes matched before edits. Historical
  artifacts remain unchanged. Remote refs matched the Director binding.

## Owner Inputs

Director is accountable for school Google Workspace, mailbox
`admin@smkdarussalamsubah.sch.id`, SA/crypt custody and revocation. SMTP binding
and mailbox receipt are not yet verified. No secret is requested through chat.

Confirmed owner inputs and pending decisions, collected together:

1. Email owner is the Director and the intended Google Workspace mailbox is
   `admin@smkdarussalamsubah.sch.id`. SMTP binding, mailbox access and an actual
   receipt remain D5 checks; this statement is not credential evidence.
2. Named alternate operator, secure transfer channel and encrypted recovery-kit
   location outside VPS and separate from the ciphertext Drive.
3. Maintenance window in WIB and incremental cost ceiling. Neither authorizes
   purchases, downtime or a restart until the exact stage packet is ratified.
4. Isolated restore target, access owner and capacity. Local Docker remains D0
   synthetic only because the workstation has about 9.66 GiB free. Recommended
   D5 target is an AWS Lightsail Jakarta OS-only VM in a school-owned account,
   with a dedicated encrypted work disk. Provisional tiers are 4 GiB/80 GB at
   USD 24/month or 8 GiB/160 GB at USD 44/month; fresh peak sizing and a proposed
   USD 5/48-hour drill ceiling require Director ratification before purchase.
5. Email plus a manual mailbox check is the proposed temporary notification
   route. It is not an independent automatic fallback when email itself is
   unavailable. Before D5, name an operator and times for direct inspection of
   n8n execution and backup evidence; verify mailbox/SMTP metadata before any
   synthetic D5 send.
6. Publication/puller authority and exact environment gates, independently
   attested absence for first publication, future image risk decisions, and
   independent reviewers for each critical packet.

## Single Batch Ledger

Status is not an operational acceptance score. SOURCE_ONLY does not mean live.

| ID | Status | Owner / dependency | Current evidence / next action |
| --- | --- | --- | --- |
| B01 | VERIFIED | Executor / D0 | Integer byte derivation, typed root-cron disposition and fresh read-only host evidence recorded; physical reclaim belongs to B08 |
| B02 | SOURCE_ONLY | Executor + source reviewer / D0 | E03-E06 source paths, real legacy baseline and compatibility chain implemented; independent review is next |
| B03 | BLOCKED | Director + organization package administrator / D1-D2 | Source supports existing/first private publication; exact publisher, puller, environment and owner absence evidence need ratification |
| B04 | BLOCKED | Director / D1 | Recovery-kit channel and delegate; no runtime key creation |
| B05 | SOURCE_ONLY | Executor + security reviewer / D2 | Backup/API/web same-byte artifact chain; no published candidate |
| B06 | BLOCKED | Recovery operator + Director / D2 | Seven-object inventory is not full-readable encrypted preservation/copy-back proof; exact object packet needs approval |
| B07 | BLOCKED | Host owner + Director / D3 | Source now captures exact writer bytes and compatibility mounts; native GC, all-writer attestation and real quiet window require an exact host packet |
| B08 | SOURCE_ONLY | Executor + Director / D3 | New 18-file UNRELEASED local feasibility bundle verified; a reviewed exact-source RELEASED bundle and physical reclaim remain future gates |
| B09 | SOURCE_ONLY | Host owner / D3,D6 | Bounded policy and samples; no growth=0 assumption |
| B10 | SOURCE_ONLY | Release operator / D4 | Images, env, helper, receipt and real staging merge binding |
| B11 | BLOCKED | Director / D5 | Restore target/custody approval; component coverage inventory |
| B12 | SOURCE_ONLY | Operator / D5 | Main-bound candidate after source promotion; one handoff |
| B13 | BLOCKED | Director / D5 | SMTP/mailbox/fallback unverified; monitor remains inactive |
| B14 | BLOCKED | Operator + reviewer / D6 | Three scheduled backups and a valid 7-day sample span cannot exist before commissioning and elapsed time |

## Reproduction Before Source Edits

- E01: prior integer observation yields 18.0011777843%, not 17.99%.
- E03: metadata verifier accepts only an existing private package; no first
  publication branch exists. A 403 or unauthoritative 404 cannot prove absence.
- E04: application preflight reads the old clean staging checkout library while
  requiring the newer compatible runtime hash. The old and develop Git blobs
  differ; copying the new library into the checkout would violate clean-base.
- E05: only a backup publisher exists. Application preflight requires API/web
  digest and exact staging revision. Chosen closure: build on the real staging
  merge SHA while deployment waits behind its environment gate.
- E06: candidate and scheduler require main source. Chosen ordering retains
  this invariant: staging acceptance, reviewed main promotion, then candidate.
  Compatibility helper is delivered separately before staging, not by pretending
  develop is production main. Drive auth remains service-account-file only.

## Read-only and Local Feasibility Evidence

- Production root crontab returned exit code `1` with `no crontab for root` at
  `2026-09-11T09:02:31Z`; this is recorded as `ABSENT`, not unreadable. It does
  not prove other schedulers absent. The same bounded observation found no active
  exact `backup.sh` argv at that instant.
- Filesystem values were 80,307,429,376 total, 14,459,756,544 available to the
  operator, and 17,782,370,304 free including reserved blocks. The operator-usable
  ratio is derived from integers, about 18.006%. This later observation supersedes
  the earlier 14,456,283,136-byte snapshot without rewriting its history.
- The live legacy script hash is
  `bc530d0a9110319684e7e4b60db56a3da1e1d979d9b1b6d8dc7887c209ff204e`.
  Container metadata confirms cron invokes it through `sh /backup.sh` at 19:00
  UTC (02:00 WIB). It has no canonical library call; the source closure therefore
  includes a hash-bound wrapper rather than claiming the library mount alone is
  sufficient.
- Local WSL Docker exercised three read-only writer mounts, tamper rejection,
  bounded legacy capture, runtime hash checks, zero owned residue, and OCI
  local/config identity separation.
  Cached image `91727198...` was test input only and is not a release candidate.
- A private local 18-file feasibility bundle was created at a WSL-local owned
  path and verified with SHA-256
  `a3df041e9c3c5a7b671894d174133896fc1a51fc2863c661a3e3e6b64734d4ba`.
  It remains `UNRELEASED`, records the D0 baseline branch point, and is not an
  exact committed-source candidate. D1 must rebuild after Git packaging rather
  than relabel or publish this local proof.
- No source build, package publication, remote file write, provider credential,
  backup, restore, GC, scheduler or runtime mutation occurred.

## Holds

No Git staging/commit/push/PR, registry publication, runtime credential, remote
probe file, cleanup/GC, restart, deployment, backup/restore, scheduler, n8n or
production mutation is authorized by this source run. Local synthetic tests and
source edits only. Director requests and later exact approvals remain distinct.
