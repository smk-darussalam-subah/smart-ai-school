# Private Help Artifacts

Wave 9 Checkpoint A reserves this directory for validated documentation artifacts. Final PDF
files are generated only after the exact staging SHA is frozen at Checkpoint B. The directory is
outside `public`; delivery uses the authenticated streaming route and an allowlisted registry.
